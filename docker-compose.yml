name: rdm_bot

services:
  rdm-bot:
    build: .
    container_name: rdm-bot
    restart: unless-stopped
    env_file:
      - .env
    environment:
      BOT_DB_PATH: /app/data/bot.db
    volumes:
      - ./data:/app/data
